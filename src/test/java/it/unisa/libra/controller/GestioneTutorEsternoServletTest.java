package it.unisa.libra.controller;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Suite.class)
@SuiteClasses({AggiungiTutorEsternoTest.class})
public class GestioneTutorEsternoServletTest {
  /*
   * private GestioneTutorEsternoServlet servlet = new GestioneTutorEsternoServlet(); private
   * HttpServletRequest request; private HttpServletResponse response;
   * 
   * @Before public void setUp() { request = mock(HttpServletRequest.class); response =
   * mock(HttpServletResponse.class); }
   * 
   * @Test public void actionNullTest() throws Exception {
   * when(request.getParameter("action")).thenReturn(null); servlet.doPost(request, response);
   * verify(response).sendError(400); }
   */
}
